/**
 * @file page.jsx
 * @description Entry route mapping to the main client-side App component. Standard page shell for the root landing and routing state.
 * @author Thabotharan Balachandran
 */
import App from '../App';

export default function Page() {
  return <App />;
}
